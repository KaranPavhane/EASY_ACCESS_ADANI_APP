# yaml-language-server: $schema=https://sap.github.io/ui5-tooling/schema/ui5.yaml.json

specVersion: "2.5"
metadata:
  name: zarmv1s3
type: application
builder:
  resources:
    excludes:
      - /test/**
      - /localService/**
  customTasks:
    - name: deploy-to-abap
      afterTask: generateCachebusterInfo
      configuration:
        target:
          url: http://vargre.tnow.com:8000/
          client: '900'
        app:
          name: ZARMV1S3
          description: Fiori app chamges
          package: ZGRAC_USER_UI5
          transport: GREK902009
        exclude:
          - /test/
